apiVersion: v1
kind: Service
metadata:
  name: doc-intelligence
  labels:
    app: doc-intelligence
spec:
  type: ClusterIP
  selector:
    app: doc-intelligence
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP